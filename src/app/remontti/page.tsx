import RenovationView from 'src/sections/_home/view/renovation-view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Remontit ja rakentaminen | Hero24',
};

export default function RenovationPage() {
  return <RenovationView />;
}
